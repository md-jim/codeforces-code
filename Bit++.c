#include<stdio.h>
int main()
{
    int n,a,j,i,x=0;
    scanf("%d",&n);
    char ch[n][2];
    for(i=0 ; i<n ; i++){
        for(j=0;j<n;j++)
        scanf("%s",&ch);

  if(ch[2]=='+')  {
    x=x+;
  }
  else if(ch[0]=='+')  {
    ++x;}

    else if(ch[0]=='-')  {
    --x;}

    else if(ch[2]=='-')  {
    x--;}}
    printf("%d",x);}











